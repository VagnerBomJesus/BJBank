rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for development (until March 2026)
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 3, 5);
    }

    // Production rules for deposits (commented out for development)
    // match /deposits/{depositId} {
    //   allow read: if request.auth != null && resource.data.userId == request.auth.uid;
    //   allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    //   allow update: if request.auth != null && resource.data.userId == request.auth.uid;
    // }
    //
    // match /external_accounts/{accountId} {
    //   allow read: if request.auth != null && resource.data.userId == request.auth.uid;
    //   allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    //   allow update: if request.auth != null && resource.data.userId == request.auth.uid;
    //   allow delete: if request.auth != null && resource.data.userId == request.auth.uid;
    // }
  }
}
