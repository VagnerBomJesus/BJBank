import { DemoHeroGeometric } from "@/components/demo-shape-hero";

export const metadata = {
  title: "Hero Geometric - BJBank Components",
  description: "Premium landing hero with animated geometric shapes",
};

export default function HeroPage() {
  return <DemoHeroGeometric />;
}
